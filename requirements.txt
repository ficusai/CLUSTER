# AI Cluster Auto-Connect — Required Dependencies
# Install all: pip install -r requirements.txt
# Or install individually: pip install PySide6

# Network Discovery
zeroconf    # mDNS service discovery for zero-config networking

# System Monitoring
psutil      # CPU, RAM, and system information

# Terminal UI (fallback when GUI not available)
rich        # Terminal dashboard with live display

# Desktop GUI (optional — for --gui mode)
PySide6     # Qt6 bindings for desktop GUI + system tray

# Configuration
PyYAML      # config.yaml parsing

# Build
pyinstaller # Build standalone executables (dev dependency)
pytest      # Tests
